{
  "name": "svelte-world-conflict-websocket",
  "version": "1.0.0",
  "description": "WebSocket worker for World Conflict multiplayer game",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --local --port 8787",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@cloudflare/workers-types": "^4.20250614.0"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
